# Firebase Functions Environment Variables
# These are managed through Firebase Secret Manager, not .env files

# To set these secrets, run:
# firebase functions:secrets:set QR_SECRET_KEY
# firebase functions:secrets:set USER_DATA_KEY

# QR_SECRET_KEY - 32-character encryption key for QR codes
# USER_DATA_KEY - 32-character encryption key for user data

# Generate random keys with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
